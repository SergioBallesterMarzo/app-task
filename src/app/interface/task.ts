export interface Task {

      id: number,
      name: string,
      status: string,
      employee: string
}

/* export enum Status {
  pendiente = "pendiente",
  en_proceso = "en proceso",
  completada = "completada"
 }
 */
